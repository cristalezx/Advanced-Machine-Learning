# 核心依赖
pydantic>=2.0
openai>=1.0          # LLM + Embedding（可替换为其他兼容 API）

# 向量库（按需选一）
qdrant-client>=1.9   # 生产推荐（Qdrant）
pymilvus>=2.5        # Milvus 2.4+ / 3.x（含 AsyncMilvusClient）

# API 服务（可选）
fastapi>=0.110
uvicorn[standard]>=0.27

# LangGraph 集成（可选）
langgraph>=0.1
langchain-core>=0.1

# 测试 / 开发
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # FastAPI 测试客户端
